server {
  listen 80;

  root /srv/www/storage/public;

  location / {
    index index.html;
    try_files $uri $uri/ =404;
  }

  location /api/ {
    proxy_pass http://127.0.0.1:60000/;
  }

  location /admin/ {
    alias /srv/www/admin/dist/;
    try_files $uri $uri/ /admin/index.html;
  }
}
